import { Box } from '@mui/material';

export default function ContactsImage() {
  return (
    <Box
      sx={{
        minHeight: '400px',
        minWidth: '100%',
        background: 'url(./img/contacts/plants-contacts.webp) 0 0 / cover no-repeat',
      }}
    ></Box>
  );
}
