import { Box } from '@mui/material';

export default function ShippingImage() {
  return (
    <Box
      sx={{
        minHeight: '400px',
        minWidth: '300px',
        background: 'url(./img/shipping/plants-shipping.webp) 0 0 / cover no-repeat',
      }}
    ></Box>
  );
}
